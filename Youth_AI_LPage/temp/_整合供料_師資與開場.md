# 供料說明 — 師資陣容 + 開場動畫（給 VS Code / Claude Code 整合進 index.html）

> 分工：**index.html 由 VS Code 端整合**；本資料夾只負責供料，不改 index.html。
> 以下檔案都已放進 repo，路徑可直接引用，**不需再去 D 槽複製**。

## 已備妥的資產（都在 repo 內）
- 頭像 13 張：`assets/faculty/`
  - 雙PM：`pm_shen.jpg`(沈瑞婷)、`pm_allen.jpg`(陳志騰)
  - 師資群：`lec_shih.jpg`(施佑林)、`lec_ma.jpg`(馬大程)、`lec_qin.jpg`(秦鼎)、`lec_xie.jpg`(謝孟芹)、`lec_sylvia.jpg`(徐小雅)
  - 助教群：`ta_lin.jpg`(林儀和)、`ta_wang.jpg`(王紹真)、`ta_hsu.jpg`(許傑閔)、`ta_hung.jpg`(洪忠育)、`ta_li.jpg`(李樂華)、`ta_wu.jpg`(吳秉翔)
- 配樂：`assets/audio/energysound-stomp-drum-percussion-513744.mp3`
- 文字資料（姓名/職稱/專長一句話）：`temp/_師資資料.md`
- **完整可預覽參考檔（HTML/CSS/JS 全在裡面，直接複製區塊即可）：`temp/_師資與開場動畫_草稿參考.html`**

## 要整進 index.html 的兩塊

### A. 師資陣容 section
- 從參考檔複製 `<section class="faculty">…</section>` 與對應的 `.faculty / .card / .avatar …` CSS。
- 三組：產學雙PM(2) → 師資群(5) → 助教群(6，含 iPAS 標籤)。
- 建議放在 #home 主視覺之後、或「三方角色」區塊附近；沿用 index.html 既有設計變數（navy/teal/gold），把參考檔的色票對應過去即可。
- 圖片保留 `loading="lazy"` 與 `alt`。

### B. 開場動畫 overlay（含配樂）
- 從參考檔複製 `#intro / #anim / #field / .skip` 的 HTML、CSS 與底部 `<script>`。
- 配樂已硬綁：`<audio id="bgm" loop src="assets/audio/energysound-stomp-drum-percussion-513744.mp3">`。
- **瀏覽器禁止無互動自動有聲播放** → 保留「▶ 播放開場」按鈕觸發；勿做 autoplay 有聲。
- 已含「略過」「Esc/空白鍵」控制、`prefers-reduced-motion` 降載。

## 整合注意
1. **只跑一次**：用 `sessionStorage`（例 `if(!sessionStorage.getItem('introSeen')){…play…} ; 結束時 setItem`），避免每次回 #home 都重播。
2. 開場 overlay 的 z-index 要高於內容、但**別蓋住既有導覽列/報名 CTA**，且結束要能完全移除（display:none）。
3. 相對路徑（GitHub Pages 子路徑）：`assets/...` 維持相對寫法即可。
4. 用語定版：對外校名「臺中科大」、職稱「智慧營運優化工程師」。
5. mp3 約 2.65MB，若在意 repo 體積，可改放 Release/CDN 再用網址引用（非必要）。

## 完成後
本機預覽桌機/手機 → 確認頭像都載入、配樂可播、動畫可略過且只播一次 → commit/push。
建議 commit：`feat: 師資陣容區塊 + #home 開場動畫與配樂`
